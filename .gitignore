# Local-only resume used for development runs; the cloud run reads RESUME_TEXT secret.
resume.txt
.env
# Legacy single-file ledgers (pre per-company split) are ignored below. The shard
# dirs that replaced them (local_data/, cloud_data/) are NOT ignored — must stay
# `git add`-able — but belong only on the `data` branch; never commit from main.
data/*.csv
*.local.csv
.ledger/
__pycache__/
*.pyc
.venv/
# Claude research/probe notes — local only (condensed facts live in Claude memory).
notes/
